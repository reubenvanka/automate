# Auto Mate ESPHome Firmware

This directory is reserved for the product-owned ESPHome assembly/configuration and focused modules.

Do not add hardware-dependent configuration until the controller, GPIO mapping, power/load topology, and sensor choices are approved in `SPECIFICATIONS.md` and `docs/architecture.md`.
