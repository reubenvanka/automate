# PCA9685 Configuration for AutoMate
# Single 16-channel PWM expander @ I2C address 0x40.
# CH0-CH13 assigned (14 channels). CH14=BACKUP_01, CH15=BACKUP_02 kept as
# NAMED backup channels so future devices can be mapped without restructuring.
# OE is strapped LOW in the module (outputs always enabled): no ESP32 OE gate,
# no pull-up, and no GPIO is allocated for OE.

i2c:
  - id: bus_a
    sda: GPIO21
    scl: GPIO22
    frequency: 50kHz

pca9685:
  - id: pca9685_hub
    i2c_id: bus_a
    address: 0x40
    frequency: 1000Hz

output:
  # --- Active channels (CH0-CH13) ---
  - platform: pca9685
    id: output_monitor1_backlight
    pca9685_id: pca9685_hub
    channel: 0
  - platform: pca9685
    id: output_desk_light1
    pca9685_id: pca9685_hub
    channel: 1
  - platform: pca9685
    id: output_table_light
    pca9685_id: pca9685_hub
    channel: 2
  - platform: pca9685
    id: output_shelf_light1
    pca9685_id: pca9685_hub
    channel: 3
  - platform: pca9685
    id: output_monitor2_backlight
    pca9685_id: pca9685_hub
    channel: 4
  - platform: pca9685
    id: output_desk_light2
    pca9685_id: pca9685_hub
    channel: 5
  - platform: pca9685
    id: output_shelf_light2
    pca9685_id: pca9685_hub
    channel: 6
  - platform: pca9685
    id: output_cupboard_bounce_light
    pca9685_id: pca9685_hub
    channel: 7
  - platform: pca9685
    id: output_cupboard_spot_light
    pca9685_id: pca9685_hub
    channel: 8
  - platform: pca9685
    id: output_door_spot_light
    pca9685_id: pca9685_hub
    channel: 9
  - platform: pca9685
    id: output_monitor3_backlight
    pca9685_id: pca9685_hub
    channel: 10
  - platform: pca9685
    id: output_monitor4_backlight
    pca9685_id: pca9685_hub
    channel: 11
  - platform: pca9685
    id: output_exhaust_fans
    pca9685_id: pca9685_hub
    channel: 12
  - platform: pca9685
    id: output_terrace_ambient_light
    pca9685_id: pca9685_hub
    channel: 13

  # --- Backup channels (CH14-CH15) - named, reserved for future devices ---
  - platform: pca9685
    id: output_backup_01
    pca9685_id: pca9685_hub
    channel: 14
  - platform: pca9685
    id: output_backup_02
    pca9685_id: pca9685_hub
    channel: 15

light:
  - platform: monochromatic
    id: light_monitor1_backlight
    name: "Monitor1 Backlight"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_monitor1_backlight
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_monitor1_backlight).current_values;
            id(monitor1_backlight_prev_state) = state.is_on();
            id(monitor1_backlight_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Monitor1 Backlight state saved: On - %s, Brightness - %.0f%%", id(monitor1_backlight_prev_state) ? "Yes" : "No", id(monitor1_backlight_prev_brightness) * 100);
  - platform: monochromatic
    id: light_desk_light1
    name: "Desk Light1"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_desk_light1
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_desk_light1).current_values;
            id(desk_light1_prev_state) = state.is_on();
            id(desk_light1_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Desk Light1 state saved: On - %s, Brightness - %.0f%%", id(desk_light1_prev_state) ? "Yes" : "No", id(desk_light1_prev_brightness) * 100);
  - platform: monochromatic
    id: light_table_light
    name: "Table Light"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_table_light
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_table_light).current_values;
            id(table_light_prev_state) = state.is_on();
            id(table_light_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Table Light state saved: On - %s, Brightness - %.0f%%", id(table_light_prev_state) ? "Yes" : "No", id(table_light_prev_brightness) * 100);
  - platform: monochromatic
    id: light_shelf_light1
    name: "Shelf Light1"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_shelf_light1
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_shelf_light1).current_values;
            id(shelf_light1_prev_state) = state.is_on();
            id(shelf_light1_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Shelf Light1 state saved: On - %s, Brightness - %.0f%%", id(shelf_light1_prev_state) ? "Yes" : "No", id(shelf_light1_prev_brightness) * 100);
  - platform: monochromatic
    id: light_monitor2_backlight
    name: "Monitor2 Backlight"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_monitor2_backlight
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_monitor2_backlight).current_values;
            id(monitor2_backlight_prev_state) = state.is_on();
            id(monitor2_backlight_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Monitor2 Backlight state saved: On - %s, Brightness - %.0f%%", id(monitor2_backlight_prev_state) ? "Yes" : "No", id(monitor2_backlight_prev_brightness) * 100);
  - platform: monochromatic
    id: light_desk_light2
    name: "Desk Light2"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_desk_light2
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_desk_light2).current_values;
            id(desk_light2_prev_state) = state.is_on();
            id(desk_light2_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Desk Light2 state saved: On - %s, Brightness - %.0f%%", id(desk_light2_prev_state) ? "Yes" : "No", id(desk_light2_prev_brightness) * 100);
  - platform: monochromatic
    id: light_shelf_light2
    name: "Shelf Light2"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_shelf_light2
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_shelf_light2).current_values;
            id(shelf_light2_prev_state) = state.is_on();
            id(shelf_light2_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Shelf Light2 state saved: On - %s, Brightness - %.0f%%", id(shelf_light2_prev_state) ? "Yes" : "No", id(shelf_light2_prev_brightness) * 100);
  - platform: monochromatic
    id: light_cupboard_bounce
    name: "Cupboard Bounce Light"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_cupboard_bounce_light
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_cupboard_bounce).current_values;
            id(cupboard_bounce_prev_state) = state.is_on();
            id(cupboard_bounce_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Cupboard Bounce Light state saved: On - %s, Brightness - %.0f%%", id(cupboard_bounce_prev_state) ? "Yes" : "No", id(cupboard_bounce_prev_brightness) * 100);
  - platform: monochromatic
    id: light_cupboard_spot
    name: "Cupboard Spot Light"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_cupboard_spot_light
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_cupboard_spot).current_values;
            id(cupboard_spot_prev_state) = state.is_on();
            id(cupboard_spot_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Cupboard Spot Light state saved: On - %s, Brightness - %.0f%%", id(cupboard_spot_prev_state) ? "Yes" : "No", id(cupboard_spot_prev_brightness) * 100);
  - platform: monochromatic
    id: light_door_spot
    name: "Door Spot"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_door_spot_light
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_door_spot).current_values;
            id(door_spot_prev_state) = state.is_on();
            id(door_spot_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Door Spot state saved: On - %s, Brightness - %.0f%%", id(door_spot_prev_state) ? "Yes" : "No", id(door_spot_prev_brightness) * 100);
  - platform: monochromatic
    id: light_monitor3_backlight
    name: "Monitor3 Backlight"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_monitor3_backlight
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_monitor3_backlight).current_values;
            id(monitor3_backlight_prev_state) = state.is_on();
            id(monitor3_backlight_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Monitor3 Backlight state saved: On - %s, Brightness - %.0f%%", id(monitor3_backlight_prev_state) ? "Yes" : "No", id(monitor3_backlight_prev_brightness) * 100);
  - platform: monochromatic
    id: light_monitor4_backlight
    name: "Monitor4 Backlight"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_monitor4_backlight
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_monitor4_backlight).current_values;
            id(monitor4_backlight_prev_state) = state.is_on();
            id(monitor4_backlight_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Monitor4 Backlight state saved: On - %s, Brightness - %.0f%%", id(monitor4_backlight_prev_state) ? "Yes" : "No", id(monitor4_backlight_prev_brightness) * 100);
  - platform: monochromatic
    id: light_exhaust_fans
    name: "Exhaust Fans"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_exhaust_fans
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_exhaust_fans).current_values;
            id(exhaust_fans_prev_state) = state.is_on();
            id(exhaust_fans_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Exhaust Fans state saved: On - %s, Brightness - %.0f%%", id(exhaust_fans_prev_state) ? "Yes" : "No", id(exhaust_fans_prev_brightness) * 100);
  - platform: monochromatic
    id: light_terrace_ambient
    name: "Terrace Ambient Light"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_terrace_ambient_light
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_terrace_ambient).current_values;
            id(terrace_ambient_prev_state) = state.is_on();
            id(terrace_ambient_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Terrace Ambient Light state saved: On - %s, Brightness - %.0f%%", id(terrace_ambient_prev_state) ? "Yes" : "No", id(terrace_ambient_prev_brightness) * 100);
  - platform: monochromatic
    id: light_backup_01
    name: "Backup 01"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_backup_01
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_backup_01).current_values;
            id(backup_01_prev_state) = state.is_on();
            id(backup_01_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Backup 01 state saved: On - %s, Brightness - %.0f%%", id(backup_01_prev_state) ? "Yes" : "No", id(backup_01_prev_brightness) * 100);
  - platform: monochromatic
    id: light_backup_02
    name: "Backup 02"
    restore_mode: RESTORE_DEFAULT_OFF
    output: output_backup_02
    on_turn_on:
      then:
        - switch.turn_on: relay_12v_driver
        - script.execute: blink_led
    on_turn_off:
      then:
        - script.execute: blink_led
    on_state:
      then:
        - lambda: |-
            auto state = id(light_backup_02).current_values;
            id(backup_02_prev_state) = state.is_on();
            id(backup_02_prev_brightness) = state.get_brightness();
            ESP_LOGD("light", "Backup 02 state saved: On - %s, Brightness - %.0f%%", id(backup_02_prev_state) ? "Yes" : "No", id(backup_02_prev_brightness) * 100);