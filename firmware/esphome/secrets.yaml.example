# AutoMate — secrets template
# Copy this file to `secrets.yaml` and fill in real values. NEVER commit `secrets.yaml`.
# Required keys are referenced in packages/core.yaml.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
static_ip: "192.168.1.200"
gateway: "192.168.1.1"
dns1: "192.168.1.1"
fallback_ap_password: "YOUR_FALLBACK_AP_PASSWORD"
api_encryption_key: "GENERATE_WITH: esphome wizard"
web_username: "YOUR_WEB_USERNAME"
web_password: "YOUR_WEB_PASSWORD"